
#![allow(non_camel_case_types)]

mod compiler;

// #[kernel]
// fn vector_add<F: Float>(a: &[F], b: &[F], c: &mut [F]) {
// 		let i = thread.x;
// 
//      if tid < a.len() {
//          c[i] = a[i] + b[i];
//      }
// }